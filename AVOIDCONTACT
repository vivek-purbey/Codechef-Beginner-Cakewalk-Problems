#include<iostream>
using namespace std;
int main() {
	int T;
	cin>>T;
	while(T--){
	    int x,y;
	    cin>>x>>y;
	    if(y==0)
	    cout<<x<<endl;
	    else if(x>y)
	    cout<<x+y<<endl;
	    else
	    cout<<(x+y)-1<<endl;
	}
	return 0;
}
