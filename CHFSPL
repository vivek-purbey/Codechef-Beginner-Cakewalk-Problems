#include<iostream>
using namespace std;
int main() {
	int T;
	cin>>T;
	while(T--){
	    int a,b,c,x,y,z;
	    cin>>a>>b>>c;
	    x=a+b,y=b+c,z=a+c;
	    if(x>=y && x>=z)
	    cout<<x<<endl;
	    else if(y>=x && y>=z)
	    cout<<y<<endl;
	    else
	    cout<<z<<endl;
	}
	return 0;
}
