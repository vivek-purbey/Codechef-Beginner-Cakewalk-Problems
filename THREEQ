#include<iostream>
using namespace std;
int main() {
	int T;
	cin>>T;
	while(T--){
	    int a1,a2,a3,b1,b2,b3,count0a=0,count1a=0,count0b=0,count1b=0;
	    cin>>a1>>a2>>a3;
	    cin>>b1>>b2>>b3;
	    if(a1==0)
	    count0a++;
	    if(a2==0)
	    count0a++;
	    if(a3==0)
	    count0a++;
	    if(a1==1)
	    count1a++;
	    if(a2==1)
	    count1a++;
	    if(a3==1)
	    count1a++;
	    if(b1==0)
	    count0b++;
	    if(b2==0)
	    count0b++;
	    if(b3==0)
	    count0b++;
	    if(b1==1)
	    count1b++;
	    if(b2==1)
	    count1b++;
	    if(b3==1)
	    count1b++;
	    if((count0a==count0b) && (count1a==count1b))
	    cout<<"Pass"<<endl;
	    else
	    cout<<"Fail"<<endl;
	}
	return 0;
}
